{
  "name": "nhs-notify-iam-webauth-ui-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:start-local-app": "npm run build --prefix ../../ && npm run start --prefix ../../",
    "test:local-ui": "playwright test --project component -c config/local.config.ts",
    "test:federation": "playwright test --project federation -c config/local.config.ts",
    "test:story": "echo 'Not implemented'"
  },
  "devDependencies": {
    "@aws-sdk/client-cognito-identity-provider": "^3.691.0",
    "@playwright/test": "^1.46.0",
    "generate-password": "^1.7.1"
  }
}
